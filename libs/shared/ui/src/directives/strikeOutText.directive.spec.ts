import { StrikeOutTextDirective } from './strikeOutText.directive';

describe('StrikeOutTextDirective', () => {
  it('should create an instance', () => {
    const directive = new StrikeOutTextDirective();
    expect(directive).toBeTruthy();
  });
});
